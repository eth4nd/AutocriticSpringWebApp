package Model.User;

/**
 * Unused class
 */
public class LoginSystem {
    /*
    public static boolean UserLogin(String username, String password, UserDatabase u)
    {
        if(u.findUser(username))
        {
            User returningUser = u.getUser(username);
            if(returningUser.getPassword().equals(password)){
                System.out.println("Login Successful! Welcome " + returningUser.getUsername());
                return true;
            }
            else
            {
                System.out.println("Incorrect Password");
                return false;
            }
        }
        else
            return false;
    }
    */
}